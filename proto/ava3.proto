// ava3 telemetry message definition; for sending data
//      from the backend to the frontend

syntax = "proto3";

package ava3;

message Telemetry {
    string type = 1;
    uint64 timestamp_ms = 2;
    uint32 id = 3;
    repeated float data = 4;
}